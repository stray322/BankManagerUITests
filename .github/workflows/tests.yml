name: Java CI with Maven, Allure and GitHub Pages

on:
  push:
    branches: [ "TestUI" ]
  pull_request:
    branches: [ "TestUI" ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      - name: Cache Maven packages
        uses: actions/cache@v4
        with:
          path: ~/.m2
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-m2

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y wget unzip xvfb

      - name: Setup Chrome
        uses: browser-actions/setup-chrome@latest

      - name: Run tests with Maven
        run: |
          export DISPLAY=:99
          Xvfb :99 -screen 0 1920x1080x24 &
          mvn clean test -B -Dheadless=true

      - name: Generate Allure Report
        run: mvn allure:report

      - name: Setup Allure History
        run: |
          mkdir -p allure-history
          cp -r target/allure-report/history/* allure-history/ || true

      - name: Upload Allure Report as Artifact
        uses: actions/upload-artifact@v4
        with:
          name: allure-report
          path: target/allure-report/
          retention-days: 7

      - name: Upload test results
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: |
            target/allure-results/
            target/surefire-reports/
          retention-days: 7

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: test
    if: ${{ github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/TestUI') }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Download Allure Report
        uses: actions/download-artifact@v4
        with:
          name: allure-report
          path: target/allure-report

      - name: Restore Allure History
        run: |
          mkdir -p target/allure-report/history
          cp -r allure-history/* target/allure-report/history/ || true

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: target/allure-report/

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
